import { } from '../actions/deck';


const initialState = {
    deck: []
};

export default function (state = initialState, { type, payload }) {
    switch (type) {

        default:
            return state;
    }
}